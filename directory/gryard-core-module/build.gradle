// 살행가능한 jar로 생성하는 옵션
// main이 없는 라이브러리에서는 false로 비활성화
bootJar.enabled = true

// 외부에서 의존하기 위한 jar로 생성하는 옵션
// main이 없는 라이브러리에서는 true로 비활성화
jar.enabled = false

dependencies {

    implementation project(":gryard-api-module")

}